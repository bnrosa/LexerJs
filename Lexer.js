class Lexer {
  constructor(input) {
    this.input = input;
    this.position = 0;
    this.line = 0;
    this.column = 0;
  }

  nextToken() {
    switch (currentLexeme) {
      case missing:
        brake;
    }
  }
}
